# Copyright 2023 Canonical Ltd.
# See LICENSE file for licensing details.
name: Update bundle

on:
  workflow_dispatch:

jobs:
  update-bundle:
    name: Update bundle
    uses: canonical/data-platform-workflows/.github/workflows/_update_bundle.yaml@lucas/new-bundle-workflow
    with:
      path-to-bundle-file: releases/latest/postgresql-k8s-bundle.yaml
    secrets:
      token: ${{ secrets.PAT }}
